import { MessagesManager } from "@/components/admin/MessagesManager";

export const metadata = {
  title: "Mensagens dos Convidados · Admin",
};

export default function AdminMessagesPage() {
  return <MessagesManager />;
}

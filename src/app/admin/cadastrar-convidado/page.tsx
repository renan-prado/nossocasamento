import { GuestManager } from "@/components/admin/GuestManager";

export const metadata = {
  title: "Gerenciar Convidados · Admin",
};

export default function AdminGuestsPage() {
  return <GuestManager />;
}

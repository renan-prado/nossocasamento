import { GiftManager } from "@/components/admin/GiftManager";

export const metadata = {
  title: "Gerenciar Presentes · Admin",
};

export default function AdminGiftsPage() {
  return <GiftManager />;
}
